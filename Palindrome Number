class Solution {
    public boolean isPalindrome(int x) {
        int n=0;
        int t=x;
        while(t>0)
        {
            int temp = t%10;
            n= n*10 + temp;
            t=t/10;
        }
        if(n==x)
            return true;
        else
            return false;
    }
}
